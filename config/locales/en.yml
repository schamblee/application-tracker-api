en:
  activerecord:
    errors:
      models:
        job_application:
          attributes:
            title:
              already_exists_for_company: "already exists for this company"
              job_posting_url:
                invalid_url: "must be a valid URL"
